<h1><%=@doctor.name%></h1>
<h2><%=@doctor.deparment%></h2>

<ul>
  <% @doctor.appointments.each do |apt|%>
    <li>
      <%=apt.appointment_datetime.date%> <%=apt.appointment_datetime.time%> <%=link_to apt.patient.name, patient_path(apt.patient) %>
    </li>
  <%end%>
</ul>
  